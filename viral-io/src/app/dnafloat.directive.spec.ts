import { DnafloatDirective } from './dnafloat.directive';

describe('DnafloatDirective', () => {
  it('should create an instance', () => {
    const directive = new DnafloatDirective();
    expect(directive).toBeTruthy();
  });
});
