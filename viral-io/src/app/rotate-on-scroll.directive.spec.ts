import { RotateOnScrollDirective } from './rotate-on-scroll.directive';

describe('RotateOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
