import { CellfloatDirective } from './cellfloat.directive';

describe('CellfloatDirective', () => {
  it('should create an instance', () => {
    const directive = new CellfloatDirective();
    expect(directive).toBeTruthy();
  });
});
